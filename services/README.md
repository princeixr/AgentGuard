# AgentGuard Services

Deployable AgentGuard product services live under this boundary.

- `agentguard_api/`: standalone policy, interception, audit, and dashboard API.

The Python ASGI entry point is `agentguard.server.app:app`.

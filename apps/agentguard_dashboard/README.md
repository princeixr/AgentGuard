# AgentGuard Dashboard

This is the standalone AgentGuard product UI.

```bash
npm install
npm run dev
npm test
npm run build
```

The dashboard communicates only with the AgentGuard server API. The independent Google
ADK personal-agent UI belongs under `examples/google_adk_agent/ui`.

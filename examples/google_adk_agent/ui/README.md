# Google ADK Agent UI

This directory is reserved for the personal agent's independent user interface.

It must communicate with the Google ADK agent application, not import AgentGuard
dashboard components. AgentGuard decisions and live traces remain visible in the
standalone AgentGuard dashboard through server APIs and event streaming.

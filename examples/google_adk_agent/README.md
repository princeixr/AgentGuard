# Google ADK Personal Agent Example

This application demonstrates how an independently deployed agent integrates with
AgentGuard. It owns the Google ADK agent implementation and is not part of the
AgentGuard server.

Run the example:

```bash
uv run examples/google_adk_agent/chat.py
```

# AgentGuard Dashboard

This is the standalone AgentGuard product UI.

```bash
npm install
npm run dev
npm test
npm run build
```

The dashboard communicates only with the AgentGuard server API. Agent-specific user
interfaces belong to their independently deployed agent projects.
